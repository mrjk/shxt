# Installation as shell manager

While shxt.sh can easily handle script dependencies, it can work with
you shell instance. 


Auto install with curl:
```bash
curl -v https://mrjk.github.io/shxt/shxt.sh | bash -s install home
bash
```



